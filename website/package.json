{
  "name": "@moe/website",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo 'Website build: Copying assets...' && mkdir -p dist && cp -r * dist/ 2>/dev/null || true",
    "lh:ci": "node ./scripts/run-lighthouse.mjs",
    "serve": "http-server . -p 4180 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "lighthouse": "^12.3.0"
  }
}
