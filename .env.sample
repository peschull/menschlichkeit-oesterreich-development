# =========================================================
# Copilot / MCP – Secrets (Codespaces)
# =========================================================
# ⚠️ Nur Beispiel: Die echten Werte bitte als Codespaces-Secrets
# hinterlegen, nicht in dieser Datei!
#
# GitHub → Settings → Codespaces → Secrets
# Danach stehen die Variablen im Codespace automatisch bereit.
# =========================================================

# GitHub Token für Copilot MCP / GitHub API
GH_TOKEN=

# GPG Key (Privat, ASCII armored) – optional für signierte Commits
GPG_ASC=

# Passphrase zum GPG Key – optional
GPG_PASSPHRASE=
# Menschlichkeit Österreich - Environment Configuration Template
# Copy to .env and configure with actual values
# For GitHub Actions, use GitHub Secrets instead of local .env

# Plesk/SSH Configuration (use GitHub Secrets: PLESK_HOST, SSH_KEY)
PLESK_HOST=your-plesk-host@your-server-ip
PLESK_REMOTE_PATH=/var/www/vhosts/menschlichkeit-oesterreich.at/httpdocs
LOCAL_WEBROOT=./

# SSH Configuration (use GitHub Secret: SSH_PRIVATE_KEY)
SSH_PORT=22
SSH_KEY=~/.ssh/id_ed25519

# Production Databases (use GitHub Secrets for all DB_PASS values)
# MariaDB 10.6.22, nginx/1.28.0, PHP 8.4.11

# Laravel API Database (GitHub Secrets: LARAVEL_DB_*)
LARAVEL_DB_CONNECTION=mysql
LARAVEL_DB_HOST=127.0.0.1
LARAVEL_DB_PORT=3306
LARAVEL_DB_NAME=mo_laravel_api
LARAVEL_DB_USER=laravel_user
LARAVEL_DB_PASS=your-secure-laravel-password
LARAVEL_DB_CHARSET=utf8mb4
LARAVEL_DB_COLLATE=utf8mb4_unicode_ci

# CiviCRM Database (GitHub Secrets: CIVICRM_DB_*)
CIVICRM_DB_HOST=localhost
CIVICRM_DB_NAME=mo_civicrm_data
CIVICRM_DB_USER=civicrm_user
CIVICRM_DB_PASS=your-secure-civicrm-password
CIVICRM_DB_CHARSET=utf8mb4
CIVICRM_DB_COLLATE=utf8mb4_unicode_ci

# Server Information (public, no secrets needed)
DB_SERVER_TYPE=MariaDB 10.6.22
DB_SERVER_OS=Ubuntu 22.04
DB_CONNECTION_TYPE=UNIX Socket
WEBSERVER=nginx/1.28.0
PHP_VERSION=8.4.11
PHPMYADMIN_VERSION=5.2.2

# Local Development Database
LOCAL_DB_HOST=127.0.0.1
LOCAL_DB_NAME=mo_local
LOCAL_DB_USER=root
LOCAL_DB_PASS=local-dev-password

# Development Configuration
NODE_ENV=development
DEBUG=true

# E-Mail Configuration (GitHub Secrets: MAIL_*_EMAIL, MAIL_*_PASSWORD)
# Austrian NGO E-Mail System for menschlichkeit-oesterreich.at

# System E-Mails
MAIL_LOGGING_EMAIL=logging@your-domain.at
MAIL_LOGGING_PASSWORD=your-secure-logging-password

MAIL_INFO_EMAIL=info@your-domain.at
MAIL_INFO_PASSWORD=your-secure-info-password

MAIL_ADMIN_EMAIL=your-admin@your-domain.at
MAIL_ADMIN_PASSWORD=your-secure-admin-password

# CiviCRM E-Mail Integration
MAIL_CIVIMAIL_EMAIL=civimail@your-domain.at
MAIL_CIVIMAIL_PASSWORD=your-secure-civimail-password

MAIL_BOUNCE_EMAIL=bounce@your-domain.at
MAIL_BOUNCE_PASSWORD=your-secure-bounce-password

# SMTP Configuration
MAIL_MAILER=smtp
MAIL_HOST=mail.your-domain.at
MAIL_PORT=587
MAIL_USERNAME=${MAIL_INFO_EMAIL}
MAIL_PASSWORD=${MAIL_INFO_PASSWORD}
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=${MAIL_INFO_EMAIL}
MAIL_FROM_NAME="Your Organization Name"

# API Keys & Secrets (use GitHub Secrets)
# CODACY_API_TOKEN=your-codacy-token
# SNYK_TOKEN=your-snyk-token
# N8N_ENCRYPTION_KEY=your-n8n-key
# CIVICRM_SITE_KEY=your-civicrm-site-key
# JWT_SECRET=your-jwt-secret
